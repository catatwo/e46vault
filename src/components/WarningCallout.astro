---
interface Props {
  title?: string;
}
const { title = 'Safety Warning' } = Astro.props;
---
<div class="warning-callout">
  <div class="warning-callout-header">
    <span>⚠️</span> {title}
  </div>
  <div class="warning-callout-body">
    <slot />
  </div>
</div>
